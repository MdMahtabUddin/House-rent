import http from 'http';

const test = async () => {
  console.log("Testing API...");
  // We cannot test without a valid session cookie.
  // We will instead directly query the database to check if everything is correct.
};

test();
